import 'dart:typed_data';

class DownloadHelper {
  static Future<void> download(Uint8List bytes, String fileName) async {
    // This is the mobile implementation stub.
    // On web, the web version will be used instead via conditional import.
    throw UnsupportedError('Cannot download on this platform');
  }
}
